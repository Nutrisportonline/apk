{
  "name": "nutrisport-android",
  "version": "1.1.0",
  "private": true,
  "description": "NutriSport Android nativo sincronizado con Firebase",
  "scripts": {
    "android:add": "cap add android",
    "sync": "cap sync android",
    "open:android": "cap open android",
    "build:debug": "cap sync android && cd android && ./gradlew assembleDebug",
    "build:release": "cap sync android && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.4",
    "@capacitor/core": "^7.4.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.4"
  }
}
